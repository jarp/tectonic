
  <div class="plate
    <%= @game.use_images? ? 'with-image' : 'basic'  %> <%= "visibility-faded found" if @current_plates.include?(plate) %>"

    plate_code="<%= plate.code %>"
    plate_id="<%= plate.id %>"

      <% if @current_game.use_images? %>
        style="background-image: url(<%= image_path("plates/#{plate.code.downcase}.jpg") %>);"
      <% end %>
    >

    <span class="owner"></span>
    <span class="plate-name"><%= plate unless @current_game.use_images? %></span>
  </div>
  <span class="plate-name"><%= plate if @current_game.use_images? %></span>
